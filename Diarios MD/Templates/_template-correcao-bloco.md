<!--
Bloco padronizado para colar no FIM de um .md de diário (após o último `## Página N`),
sem alterar o texto OCR original.
-->

## ✏️ Correções de texto

> Lista de prováveis erros de OCR detectados neste diário. **Não foi alterado o texto OCR
> bruto** (regra de ouro). Esta seção serve para localizar e relacionar grafias divergentes
> de um mesmo nome/termo.

| Página | Antes (OCR) | Depois (provável) | Tipo | Confiança |
|---|---|---|---|---|
| N | `Felıx` | `Felix` | nome | alta |
| N | `Antônlo` | `Antônio` | nome | alta |
| N | `Sllva` | `Silva` | nome | alta |

> Tipos: `nome`, `cargo`, `orgao`, `empresa`, `cnpj`, `cpf`, `processo`, `valor`, `data`, `outro`.
> Confiança: `alta` (padrão OCR conhecido), `media` (contexto sugere), `baixa` (chute).
