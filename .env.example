# ============================================
# .env — SECRETS LOCAIS (NUNCA COMMITAR)
# Copie este arquivo: cp .env.example .env
# ============================================

# ----- LLM / Embeddings -----
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# ----- App -----
JWT_SECRET=change-me-please-openssl-rand-hex-32
APP_ENV=development
LOG_LEVEL=info

# ----- Postgres -----
POSTGRES_USER=invest
POSTGRES_PASSWORD=invest_dev_pwd
POSTGRES_DB=investigacao
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# ----- ChromaDB -----
CHROMA_HOST=chromadb
CHROMA_PORT=8000

# ----- MinIO (S3 compatível) -----
MINIO_ROOT_USER=invest
MINIO_ROOT_PASSWORD=invest_dev_pwd
MINIO_ENDPOINT=http://minio:9000
MINIO_BUCKET=casos

# ----- Frontend -----
NEXT_PUBLIC_API_URL=http://localhost:8000

# ----- Infra do Rafael (NÃO commitar) -----
# Servidor próprio (Tailscale)
TAILSCALE_HOST=100.95.227.38
TAILSCALE_USER=rafael
# Coolify
COOLIFY_BASE_URL=http://100.95.227.38:8000
COOLIFY_API_TOKEN=
